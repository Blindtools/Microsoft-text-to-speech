# List of all supported voices can be obtained using edge-tts CLI: `edge-tts --list-voices`
# For simplicity, we'll allow any voice in the request.

DEFAULT_VOICE = "en-US-GuyNeural"
DEFAULT_OUTPUT_FILE = "output.mp3"
